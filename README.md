# 患者リハビリチェックアプリ

患者が自宅で実施・痛み・メモを記録し、来院時に担当PTと振り返る端末内保存型PWAです。

## 2026-09-25 TASK-012 処方入力の回帰修正（v48・未デプロイ）

- 一括・個別の新規処方に種目別の量・時間・セット・日内回数・週頻度を入力済みの編集案として表示。保存標準の空/null値は初期案を消さず、有効な値と明示曜日を優先します。既存処方と編集中の値は補完し直しません。
- 週2日は月木、週3日は月水金、週7日は全曜日を初期選択します。曜日を初期選択しない従来設計を撤回しました。編集後の曜日は自動変更せず、週頻度との不一致を表示します。種目選択・承認・患者確認・個別許可は自動付与しません。
- 保存エラーは最初の対象欄に関連エラーを表示し、非表示の選択種目を表示、親の詳細欄を展開してスクロール・フォーカスします。共通担当者欄も対象です。修正時に再検証し、エラー時は部分保存しません。
- 109種目、4 retiredの履歴保持、KOA18種、印刷3種/ページを維持。SWキャッシュはv48。

## 2026-09-25 TASK-011 4運動の選択対象からの削除（v47・未デプロイ）

- S06／P02／P03／P10を全疾患・全段階・一般／競技・検索・保存済みセットの選択候補から除外しました。保留カードや代替種目は追加しません。
- 利用対象は109運動・109画像。113定義のうち4件は `status: retired` として履歴参照用に保持し、新規処方・再開は拒否します。旧下書きや標準指示からも追加できません。保存済み処方・記録・テンプレートの元データは削除しません。
- KOAは各段階6種、計18種を維持。他疾患は除外後の実数を表示します。従来チェック式UI・初期値・一括保存・印刷1ページ3種目を維持し、SWを公開済みv46からv47へ更新しました。
- 以下は過去の更新記録です。4種の保留表示・450割当の記述は本更新前の状態です。

## 2026-09-25 TASK-009 一括選択（公開対象 v46・未デプロイ）

- 新規・既存患者とも「疾患別メニューから選ぶ」の1入口。一般14／競技11、25カテゴリー・113定義・450割当の新版だけを候補に表示します。旧疾患一覧は撤去し、旧処方・過去記録・個別編集の互換と、保存した独自セットの管理を残しています。
- 疾患カード → 初級／中級／上級の6候補 → チェックで複数選択 → 選択種目の「指示と必要な確認」を開く → 選択分を一括確認 → 一括保存／QRの順に操作します。「＋ 運動を選んで追加」は同じ入口へ移動します。
- 段階を越えた選択、運動名検索、選択済みだけ表示、画像プレビューで入力を保持します。閉じると未保存分を取り消します。患者切替でもドラフトを破棄します。既存種目は同じIDで更新し、過去の記録を維持します。
- 回数・セット・保持／時間・頻度・休息・用具は提供仕様の明示的な対応表（`clinical-rules.js`）による**編集案**。範囲は下限値を出発点とし、個別指定を共通プリセットより優先します。S01は20秒×2、P06は2秒静止、K17は各方向5歩×2。方向・往復・歩行と軽走の内訳も量の補足として保存します。
- 同じ新版IDで先生が保存した標準量・用具を優先します。旧キーや名前一致からの流用はありません。左右・追加負荷・許可範囲・術後条件・ゲート・個別競技計画は自動設定しません。週頻度は曜日指定とは別で、曜日は初期未選択です。
- 担当者・確認日は選択全種目について一度入力し、適用を明示確認します。定義承認と患者別確認は区別し、各種目の画像適合・必要許可・個別確認が必要です。編集するとその種目の画像適合と患者別確認を解除します。
- 全選択を検証後、`commitMenu`を1回だけ呼び出します。未確認・入力エラーは1種も保存せず、端末保存失敗も既存の復元処理で元に戻します。保存前は実処方に反映しません。
- KOA18種、保留4件（S06／P02／P03／P10）、5図注、印刷1ページ3種・最大6種、QR・動画・旧記録を維持します。
- 公開対象は既存の実行用ファイル・assets・vendor・images一式と、更新した `app-v2.js`、`clinical-ui.js`、`clinical-rules.js`、`app-v2.css`、`sw.js`（v46）。連携ファイル・テスト・`.test-artifacts`は公開対象外です。コミット・push・デプロイはこのタスクでは実施しません。
- 単体確認：`node --test --test-isolation=none tests/*.test.cjs`。実ブラウザ確認：親側で `node tests/clinical-browser.cjs` と `node tests/browser-smoke.cjs` を実行してください。新版の複数選択・入力保持・失敗時部分保存なし・一括QR・再読込・320/390px表示のシナリオを更新しています。ブラウザ未実施はPASSとして扱いません。

## 2026-09-25 公開前更新（v44）

- 新カタログは25カテゴリー・113定義・109採用候補画像。KOAは各段階6種目。S06／P02／P03／P10の4件は保留・利用不可を維持します。
- 臨床レビューと患者ごとのPT確認後に処方します。臨床承認は自動付与せず、旧処方を保持します。
- 新旧・混在とも印刷はA4縦1ページ3種目、最大6種目で2ページ。個別指示・手順・図注・注意・画像を保持し、長い指示が収まらない場合や画像読込失敗時は印刷を停止します。

## 2026-09-19 運動別の初期値（v29）

- 全46種目の標準指示のたたき台を追加。疾患メニューの新規入力時に回数・保持・支え方等を表示します。先生が保存した標準指示を優先し、既存患者の指示は変更しません。
- 左右、重りやバンドの負荷、保護が必要な肩の範囲などは個別に確認・入力する欄を残します。初期値があっても種目は未選択で、患者への反映には確認・保存が必要です。
- 数値は処方入力用の編集案で、ガイドラインで確立した標準用量ではありません。[全46種目の初期値と一次資料・適用条件](BUILTIN_DEFAULTS.md)。
- 初期値はオフラインでも使用できます。41件のデータテストとブラウザ回帰テストで網羅性・共有・履歴・入力制御を確認しています。

## 2026-09-19 処方入力の簡略化（v28）

- 個別指示をタップ選択式へ変更。選択肢にない指示は「その他・自由入力」から入力できます。左右各の回数は専用の選択肢で明示します。
- 必要な種目にチェックしたときだけ個別指示欄を表示します。
- 種目ごとの標準指示を、この端末のカスタムテンプレートとして保存・再利用できます。標準指示は疾患メニューの候補には混ぜず、種目の入力欄から呼び出します。バックアップとテンプレート書き出しに含まれます。
- 同じ患者の同じ種目の前回指示も再利用できます。コピーは個別指示と曜日のみで、疾患注意や個別メモを上書きしません。
- 医学的な標準回数・負荷を自動設定するものではありません。担当者が標準指示を保存し、呼び出した後に患者ごとの左右・負荷・曜日を確認して保存します。
- 38件のデータテストとブラウザ回帰テストが成功。標準指示の保存・再利用、前回指示、選択と自由入力の同期、320px幅のボタン表示も確認しました。

## 2026-09-19 患者・PTレビューに基づく改善（v27）

- 疾患追加は一旦停止。左右、回数、セット、保持時間、1日の頻度、負荷、支え方を種目ごとに設定し、実施曜日も明示的に確認します。不要な項目は「該当なし」と入力します。
- 既存の指示文・処方・記録は残します。旧処方の再共有時は未指定欄を確認してください。共有前に患者表示を確認する画面を追加しました。
- 個別指示・疾患の注意・種目の注意を区別し、開始前の注意をイラストより前へ表示。目的・用具・相談先・痛みを記録する場面も確認できます。
- 手順の閉じるボタンを固定し、末尾に戻る操作を追加。背景スクロールを固定し、閉じた後に元の位置へ戻します。
- 実施完了数と記録数を分離。「一部できた」「痛みで休んだ」も記録済みとして表示し、全種目の様子を記録したら保存済みと案内します。
- QRは通常の誤り訂正Mで収まらない場合にLで再試行。指示を削らず、なお大きすぎる場合は設定URLで受け渡します。実カメラでの読取検証は別途必要です。
- Chromeの検証用プロファイルはOSの一時フォルダに作成し、終了後に削除します。OneDriveには蓄積しません。

## 2026-09-18 更新

- 第3回：肩関節前方脱臼の整復後・SLAP損傷の保存療法に各4候補を追加。前方脱臼は骨折なし・運動許可済みの場合に限定します。後縦靭帯骨化症は神経所見や転倒リスク等を踏まえた個別管理が必要なため、汎用運動メニューを作りません。「リハビリ不要」という分類ではありません。

- 第2回：腱板断裂6候補・頚椎椎間板ヘルニア4候補を追加。診察で保存療法が選ばれ、PTが実施可能と評価した場合に使用します。既存の運動・画像を再利用し、無理に種目数を増やしません。術後や脊髄症など個別の管理が必要な状態へ、このメニューを流用しません。

- HP疾患記事から首・肩を優先し、頚椎症7候補・肩インピンジメント8候補を追加。新しい運動6種と共通の既存種目を組み合わせ、疾患別の注意文を患者画面・設定QRへ引き継ぎます。[追加状況と次回以降の対象](ARTICLE_EXPANSION.md)。

- 分担レビューで見つかった5件を修正：別タブの更新後に古い内容で上書きしようとすると保存を停止し、再読込を案内。全種目削除予定でも本日の記録を表示。旧版記録の保存コピーを保持。「イラストなし」の明示設定と空URLの入力案内を修正。
- 膝OAなどの類似運動を同系統の方法・負荷段階として説明し、選択中・保存済み処方の両方に併用時の確認文を表示。種目や処方を自動削除しません。
- 膝つきプランク、手首伸筋ストレッチ、前腕回旋の3枚を描き直し、別担当が再確認。手順の支持位置・図の場面説明も改善しました。[修正結果](REVIEW_RESULTS.md)。

- 当日記録後に3→7種目などへ変更した場合、セラピスト画面に適用時期と「保存したメニューを今日から反映」を表示。内容・回数・曜日が同じ種目の実施状況と、痛み・メモを保持します。追加・内容変更した種目は未記録にし、変更前の記録はカレンダーとバックアップに残します。

- 日付欄の再修正：表示を通常テキストにし、その上の透明なネイティブ入力で日付選択を維持。ブラウザ固有の日付描画が枠を広げない構造へ変更。次回来院日の解除操作も追加。320px幅・入力の最小幅400pxの模擬条件で検証（iPhone実機は未確認）。

- スマホでは開始日・次回来院日を縦並びにし、日付入力のはみ出しを修正。ヘッダーとモーダル上部にノッチ用の余白を確保し、長い表示名を折り返します。

- スマホ下部のタブに末尾の内容が隠れる問題を修正。本文にタブとホームバーの分の余白を確保し、種目選択の更新ボタンも重ならない配置に変更。

- 五十肩・腰痛・膝OA・テニス肘は各10候補、頚椎症は7候補、肩インピンジメントは8候補、腱板断裂は6候補、頚椎椎間板ヘルニア・肩関節前方脱臼の整復後・SLAP損傷は各4候補。10領域で固有46種目を共有する選択式メニュー。
- テニス肘は肘・前腕の軽い運動、ストレッチ、等尺性運動、重りを使う運動、軽い握力練習から選択。手首の筋力種目は段階違いの候補で、重複する負荷をPTが調整します。
- オリジナルAIイラスト46点、拡大表示、日本語の手順、注意点。
- PT画面は目的別に分類し、難易度・種目名・用具で絞り込み可能。非表示になった選択種目も保持し、選択件数に表示します。
- 各種目の目的・用具・選択時の注意を追加。難易度は動作の目安であり、臨床評価尺度ではありません。[種目別の一覧と参考資料](MENU_GUIDE.md)。
- PTが選んだ種目に回数・時間を入力して処方。自動で全種目を患者に処方しません。
- 動画はPTが確認したHTTPS URLを種目ごとに登録可能。外部サイトで開きます。生成動画は含みません。
- 毎日の実施を「できた・一部できた・痛みで休んだ・忘れた」に分けて記録。
- 痛み0と未記録を区別。メモは入力時に保存。
- 日付別の処方履歴と記録時のメニューを保持。記録済みなら通常は翌日から更新し、PTが必要に応じて今日から反映できます。当日も実施曜日に合う種目だけを表示します。
- 開始前・休養日・旧版のメニュー不明日は達成率の分母から除外。
- 別患者の設定は、現在の患者記録を退避して読み込み。同一患者は識別IDで判定。
- 設定QRから氏名・年齢・診断名・PINを除外。フラグメント `#d=` を使用。
- QR生成・読込ライブラリと画像を同梱。初回オンライン読込完了後はオフラインで利用可能。
- カレンダー月末移動、痛みグラフの日付間隔、入力HTMLの扱いも修正。

## 手元で開く

このフォルダで次を実行します（Pythonを使用）。

```powershell
python -m http.server 8765 --bind 127.0.0.1
```

ブラウザで http://127.0.0.1:8765/ を開いてください。ファイルの直接ダブルクリックではQRやPWA機能が正しく動作しない場合があります。

初回は「スタッフ：メニューを設定」または画面上部を2秒長押し。PIN未設定なら空欄で確認します。

1. 患者の呼び名・開始日を設定。
2. 疾患を選び、目的・難易度・検索で候補を絞ります。必要な種目にチェックし、個別指示7項目と曜日を確認。「イラスト・手順を確認」で患者表示も確認します。
3. 個別の注意点・動画URLを必要に応じて編集し、相談先・痛みを記録する場面・再開の個別指示を設定。共有前の確認画面で内容を確認します。
4. 患者の端末へアプリ追加用QRを案内し、アプリ内カメラで設定QRを読み込む。
5. 受信端末で患者の呼び名を設定。日々の記録は来院時のレポートで確認。

## データの扱い

`localStorage.rehab_v2` に設定・記録・カスタムテンプレート・退避記録をまとめて保存します。一度の書き込みで更新し、保存失敗時は直前の状態へ戻します。

旧版の3キーは初回移行時に `rehab_legacy_backup` にそのまま退避し、既存キーも削除しません。当時の処方を復元できない旧記録は、チェック・痛み・メモを保持して「旧記録」と表示します。当時の達成率を推定して表示することはありません。

患者側の「自分の記録をバックアップ」は現在の患者だけを出力します。スタッフの「端末全体をバックアップ」は退避記録・テンプレートも含みます。PINはバックアップやQRで転送せず、端末固有の簡易ロックとして扱います。

PINは認証基盤や暗号化ではありません。QRには運動内容と個別の注意文が含まれます。個別の注意文に氏名などを書かず、本人に直接渡してください。既存の旧形式URLの受信は維持していますが、新しく生成するURLに患者プロフィールは含みません。

## 運動資料と画像

回数・保持時間は一律に決めず、担当PTが評価して設定します。イラストは説明用で、個別の可動域・病期・術後制限を示すものではありません。患者への配布前に担当PTが手順と図を確認してください。

参考資料（2026-09-18確認、画像は転載せず独自生成）：

- [AAOS：肩の運動プログラム](https://orthoinfo.aaos.org/globalassets/pdfs/2017-rehab_shoulder.pdf)
- [AAOS：Frozen Shoulder](https://www.orthoinfo.org/diseases--conditions/frozen-shoulder)
- [AAOS：Spine Conditioning Program](https://www.orthoinfo.org/recovery/spine-conditioning-program/)
- [Royal Orthopaedic Hospital：Knee OA exercises](https://roh.nhs.uk/services-information/therapy/exercises-for-osteoarthritis-of-the-knee)
- [AAOS：Epicondylitis exercise program](https://orthoinfo.aaos.org/globalassets/pdfs/2022-therapeutic-exercise-program-for-epicondylitis.pdf)
- [NHS Bexley：Tennis elbow](https://msk-bexley.nhs.uk/conditions/elbow-pain/tennis-elbow)

画像：`assets/exercises/*.png`（アプリで使用する46点。以前の修正版も保存）
生成方法：組み込み `image_gen`。プロンプト：`assets/exercises/PROMPTS.json`。

46点とも同じ20代前半の若い男性モデルに統一しました。振り子体操の画像を人物の基準として、顔・髪型・体格・青緑のTシャツ・紺の長ズボン・白〜グレーの靴を合わせています。`illustration-preview.html` で全画像を一覧できます。テニス肘の10点も組み込み `image_gen` で制作し、手首や手が見やすい構図にしています。プロンプトと修正指示を保存しています。

追加種目はOxford University Hospitals、Cambridge University Hospitals、NHSの腰痛・膝OA資料なども参考にしています。各種目の出典は `MENU_GUIDE.md` とアプリ内の手順から開けます。姿勢・支持物などを調整した説明を含みます。既存患者の処方に種目を自動追加することはありません。

## 構成と確認

- `index.html`：画面枠と基本スタイル
- `ui.js`：日付・QR・初回導入などのUI
- `core.js`：入力検証、移行、処方履歴、集計の純粋関数
- `app-v2.js`：画面と保存・患者切り替え
- `exercises.js`：疾患・種目・参考資料
- `disease-library.js`：疾患ごとの共有種目・患者へ渡す注意文
- `sw.js`：アプリ範囲のオフラインキャッシュ

```powershell
node tests/core.test.cjs
node tests/state-regressions.test.cjs
node tests/exercises.test.cjs
node tests/disease-library.test.cjs
node tests/prescription.test.cjs
node tests/browser-smoke.cjs
```

ブラウザテストはWindowsのChromeを非表示の専用プロファイルで起動し、架空データだけを使用します。プロファイルはOSの一時フォルダに作成し、終了後に削除します。スクリーンショットは `.test-artifacts/` に保存されます。テスト用プロファイルを通常の患者運用に使用しないでください。

保存競合・旧記録保持・空メニュー・画像なし設定などの回帰テスト7項目も追加済みです。他タブの更新を検出した際は最新データを読み直してください。保存前の比較は、完全に同時のlocalStorage書込みの原子性まで保証するものではありません。

確認済み：データ処理11項目、保存等の回帰7項目、種目データ・画像・キャッシュ対象5項目、疾患別の共有・注意文引継ぎ10項目、Chromeでの画面・QR・保存・オフライン動作84項目、計117項目。QRカメラの起動待ちでキャンセルした場合と、権限拒否後の終了処理も模擬カメラで確認しています。スマホ実機のカメラ権限・iPhoneのホーム画面追加・実際の運動指導は、この自動確認の範囲外です。

公開先は https://patient-rehab-app.vercel.app/ です。GitHubの `sts00828-web/patient-rehab-app` の `main` への更新をVercelが自動配信します。公開時は実行用ファイルとassets・vendorを一緒に配置し、更新ごとに `sw.js` のキャッシュバージョンを上げてください。今回の公開対象は `v46` です。更新が表示されないときは、オンラインでアプリを開いた後、一度閉じて開き直してください。端末の記録を残すため、サイトデータの削除は不要です。


### TASK-014 / SW v49：シンプル処方と患側

患者ID・氏名の直下に患側（未選択／右／左／両側／左右指定なし）を常時表示します。患側は端末保存・バックアップ・退避復元に保持され、新規候補の実施側へ引き継がれます。本文にある両側・交互動作などは種目別の例外を優先し、実施側は「個別に変更」で調整できます。患側変更は保存済み処方・履歴・手動の種目別指定を書き換えません。

新版の保存・編集は `clinicalV02.mode: "simple"` を明示し、担当者・承認日・画像適合・G1〜G8・術後詳細・6種超理由・保持なしの確認チェックを要求しません。承認済み記録は自動生成せず、旧承認記録と履歴は保持します。左右・量・曜日の入力検証は継続し、負荷・範囲・支持・注意事項は任意入力です。左処方も共通動作例の画像で利用でき、患者表示・印刷では図より各運動の実施側を優先する説明を表示します。画像の反転・採用変更は行っていません。

有効109種目、KOA各段階6種、週2＝月木／週3＝月水金／週7＝毎日、印刷3種／ページ、氏名・カルテIDのQR除外、削除済みS06/P02/P03/P10の拒否を継続します。

確認コマンド：`node --test --test-isolation=none tests/*.test.cjs`、`node tests/clinical-browser.cjs`。後者は実ブラウザで承認チェックなしの保存→QR→患者開始・完了、320/390幅、エラーフォーカス、保存失敗の原子性を確認します（ブラウザ起動制限下では未検証として扱います）。

### TASK-016 / SW v50：選択後の指示欄をクリック展開

運動を選択しても指示欄は閉じたままです。「回数・曜日などを変更」を押すと概要・注意・入力欄を開けます。初期値のまま選択のみで保存でき、開閉しても編集値は保持されます。入力エラー時は該当欄を自動展開してフォーカスします。個別編集モーダルは従来どおりです。
