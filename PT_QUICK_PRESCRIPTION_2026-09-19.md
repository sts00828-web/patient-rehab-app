# PT処方の簡略化・任意の患者情報（2026-09-19）

## 今回の変更

- 患者ID・カルテ番号 `chartId` を追加。氏名とともに任意で、空欄でも保存・QR発行が可能。
- `chartId` は先頭の0を維持する文字列。内部の処方識別用 `patientId` とは別。カルテ番号の入力で他の記録を自動統合しない。
- 患者情報・相談先・日付などは任意設定に集約。新規は運動候補、既存処方は現在の運動を先に表示。
- 標準値が入った指示は値を表示した折りたたみ式に変更。左右や個別の負荷など空欄の項目は開いたまま表示し、保存時に必須チェックを維持。
- 保存ボタンを指示確定の操作とし、種目ごとの確認チェックを廃止。未設定項目のある指示を自動で処方済みにしない。
- 「確認して保存・QRを表示」で一括操作。保存後の同じ指示への共有前チェックを重ねない。
- 標準候補から既存処方に追加可能。既存の種目ID・個別指示を維持し、同じ運動の追加は拒否。
- 保存済み運動セットは全種目を選択した状態で開く。ただし前患者の左右をそのまま処方しない。
- 診断名は候補選択で上書きしない。
- PIN未設定なら空欄確認画面を省略。設定済みPINは従来どおり確認。
- サービスワーカー v30。

## データの扱い

カルテ番号・氏名はこの端末内とバックアップ・退避記録に保存する。患者用QR/URLには含めない。既存の記録・当日変更の履歴保護・保存失敗時の復元を維持。

同じ患者の別部位は別の処方として作成できるが、患者マスターと処方を分離した共通データベースは未実装。患者から医院への報告送信、デジカル連携、Tampermonkeyスクリプトも未実装。

## 検証

- Node単体・状態回帰テスト42件合格。
- 非表示Chromeで既存の患者画面、3→7種目変更、保存・復元、QR、46枚のオフライン画像と追加仕様のブラウザテストを実行。
- 390×844で実マウスイベントを使い、膝OAの3種目を選択して右側を指定、ID・氏名空欄でQR発行を確認。
- 上記の操作は初回案内を閉じる・スタッフ画面を開く操作を含め10タップ。PT画面からは8タップ、文字入力0項目。前回の同等候補3種目の計測は18タップ・1入力。スクロールは自動位置合わせで、実際のPTの所要秒数は未計測。動作指導・臨床判断の時間を含まない。
- 実入力した架空のID・氏名の保存と、患者URLに含まれないことを確認。
- ブラウザの一時プロファイルはOneDrive外に作成し、終了時に削除。

## 将来のデジカル連携

まず患者報告を確認可能な文章にまとめ、PTがコピーしてカルテへ貼り付ける運用を想定。Tampermonkeyは画面への補助UI追加や入力補助の候補になるが、デジカルの実画面・入力イベント・施設の利用条件の確認が必要。画面変更で動作が変わり得るため、正式APIと同等の安定性は保証できない。

将来の補助ツールでは、カルテ番号・氏名の照合、PTによる内容確認、カルテの手動確定を前提とする。現在の任意入力IDは認証手段ではなく、患者からの報告受信には別途本人・医院のアクセス制御を設計する。

参考：[Tampermonkey公式ドキュメント](https://www.tampermonkey.net/documentation.php)

## 運動候補一覧の追加改善（v34）

- 運動名・目的・小さいイラストのコンパクトな一覧。画像を押すと従来の手順を確認できる。
- 詳細な指示は選択した運動で必要時に開く。左右は一覧の選択欄で直接変更できる。
- 「選択済みだけ表示」と、上部に固定した「選択分を確認」「保存・QR」を追加。
- 疾患の注意事項、種目の選択上の注意、検索フィルターは開閉式。説明そのものは保持する。
- 保存済みの個別指示・記録を変更しない。未入力欄への移動では詳細欄も自動で開く。
- 390×844の膝OA候補10種目で、モーダル内容高が5,053pxから2,191pxへ約57%減少。候補カードの高さ合計は3,905.5pxから1,405px。操作時間や臨床判断時間の測定ではない。
- 320pxで固定操作の表示と横はみ出しがないことを確認。単体・状態テスト45件、既存ブラウザテストと選択済みフィルター・左右変更・エラー誘導・オフライン再読込を確認。

## 候補の並び順統一（v35）

- 全疾患の候補を「ストレッチ→関節を動かす運動→筋力トレーニング→立位・日常動作」に統一。検索の目的分類も一致させる。
- 筋力候補は重り・バンドなし、柔らかいボール、重り・バンドありの順。同一種目の相対的な順番は疾患をまたいで共通。
- 新しく選択して保存する種目も候補一覧の順に並ぶ。既存処方に追加する場合は、従来の種目順を保ち、新しい追加分を共通順で末尾に加える。
- 既存の患者処方・過去記録・元のマスター配列の順序は自動変更しない。分類は探しやすくするための表示ルールで、全患者に共通する実施順や負荷の強さを規定するものではない。
- 単体・状態テスト46件、候補・新規処方の順序、各疾患のQRでの順序保持と患者側読込、オフライン動作を確認。
