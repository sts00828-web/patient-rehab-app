# 患者向け運動プリント

## 使い方

1. セラピスト画面で運動と個別指示を保存する。
2. 「患者さんへ渡す」→「紙で渡す（印刷・最大6種目）」を押す。
3. 印刷する運動を1〜6種目選び、「印刷用画面を開く」を押す。
4. 内容を確認し、「印刷・PDFに保存」を押す。
5. A4・縦・倍率100%、ブラウザのヘッダーとフッターはオフにする。両面はプリンターの設定で「長辺とじ」を選ぶ。

1ページ3種目で、最大2ページ。両面印刷の指定はブラウザ／プリンター側で行う。両面非対応のプリンターでは2枚に印刷して渡せる。

## 掲載内容

イラスト、運動手順、回数、セット、保持時間、頻度、左右、負荷・範囲、支え方、実施曜日、補足、注意文、相談先、再開指示を載せる。患者名・患者ID・担当PT・次回来院日は入力済みの場合に掲載する。氏名空欄の場合は手書き用の欄を表示する。

患者の実施記録・QRコード・PINは印刷しない。印刷対象を選んでも処方や実施記録は変更しない。7種目以上の処方は印刷対象を明示的に選ぶ。

## 制限

- 未設定の個別指示がある場合は、先に処方を確認・保存する。
- イラストがない独自種目は、その旨を明記して文章で印刷する。
- 画像読込失敗や長文のはみ出しは検出して印刷ボタンを無効にする。文章を勝手に切り詰めない。
- ポップアップをブロックした場合は、当サイトのポップアップを許可する。
- ブラウザ内で作成し、外部のPDF生成サービスに送信しない。

## 検証（2026-09-19）

Chromeで実際の別ウィンドウを開き、1種目のPDFが1ページ、4・6種目が2ページになることを確認。全10疾患の標準候補を6種目以内に分けて、印刷枠への収まりを確認。未設定指示・0件・7件・長文・画像読込失敗も確認。PDFからの文字確認と描画確認を実施。実機プリンターの両面給紙は未検証。

実装：`exercise-print.js`。テスト：`tests/exercise-print.test.cjs`、`tests/print-browser.cjs`。サービスワーカー v32。
